## Summary

<!-- What changed and why. -->

## Test plan

- [ ] `uv run ruff check .`
- [ ] `uv run pytest -m "not conformance"`
- [ ] Docs updated if YAML / CLI / integrations changed
- [ ] No secrets or `.env` files in the diff
