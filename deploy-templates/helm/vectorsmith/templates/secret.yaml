apiVersion: v1
kind: Secret
metadata:
  name: {{ include "vectorsmith.fullname" . }}-placeholder
  labels:
    {{- include "vectorsmith.labels" . | nindent 4 }}
type: Opaque
stringData:
  note: "Replace with envFileSecret ({{ .Values.envFileSecret }}) holding QDRANT_URL and keys."
