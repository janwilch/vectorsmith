apiVersion: v1
kind: Service
metadata:
  name: {{ include "vectorsmith.fullname" . }}
  labels:
    {{- include "vectorsmith.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "vectorsmith.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
