apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vectorsmith.fullname" . }}-tools
  labels:
    {{- include "vectorsmith.labels" . | nindent 4 }}
data:
  tools.yaml: |
    tds_version: "2"
    connections:
      main:
        backend: qdrant
        url: ${QDRANT_URL}
    tools: []
